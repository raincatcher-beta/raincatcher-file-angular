/**
 * Default module configuration
 *
 * @type {{globalFileList: boolean, mountPath: string}}
 */
module.exports = defaultModuleConfig = {
  // Render file list for all users
  globalFileList: false
};